[pytest]
# Tests are pure (no Flask / pyglet / GStreamer), so they run on any box with
# just pytest installed.  ``pythonpath = .`` puts the repo root on sys.path so
# ``import paths`` / ``from dnd_display...`` resolve regardless of how pytest
# is invoked.
testpaths = tests
pythonpath = .
addopts = -q
